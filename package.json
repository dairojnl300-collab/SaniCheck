{
  "name": "sanicheck-ecodesa",
  "version": "4.13.1",
  "description": "Inspección PSB móvil — ECODESA",
  "private": true,
  "scripts": {
    "sync-version": "node scripts/sync-version.js",
    "generate-portal-config": "node scripts/generate-portal-config-secrets.js",
    "build": "node scripts/sync-version.js && node scripts/generate-portal-config-secrets.js && node build.js",
    "dev": "python -m http.server 8080 --directory app",
    "verify-sw": "node scripts/verify-sw-offline.mjs"
  },
  "devDependencies": {
    "terser": "^5.48.0",
    "puppeteer": "^25.9.0"
  }
}
